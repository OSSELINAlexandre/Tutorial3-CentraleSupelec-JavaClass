package Exercise2;

public class User {
	
//	This class doesn't sem really usefull : we could have just set a String in Student. But, for the app, it could be used as an identifier for other classes
//	(For identifying the lecturer for instance).
	private String UserId;

	public User(String userId) {
		super();
		UserId = userId;
	}
	
	
}
